<tr>
  <td>
    <p>{{track.name}}</p>
  </td>
  <td>
    <audio controls src="{{track.preview_url}}"></audio>
  </td>
  <td>
    <form action="/playlist/{{playlistId}}/delete-track" method="POST">
      <input hidden type="text" name="track" value="{{track.name}}" />
      <button type="submit">Delete</button>
    </form>
  </td>
</tr>